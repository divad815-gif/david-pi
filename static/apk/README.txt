Place the reproducibly built Android APK here as:

  david-pi-backup.apk

The authenticated /device-backup page calculates and displays its SHA-256.
Do not commit signing keystores, passwords, or keystore.properties.
