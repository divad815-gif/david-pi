# Release artifacts

Local development images may omit Android artifacts. The portal then hides the APK download. Stable publication requires the signed APK and verified attestation under `artifacts/android/`; signing keys never belong here.
